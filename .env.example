# WARNING: Demo mode sends OpenRouter requests directly from the browser and exposes the API key to end users.
# WARNING: Use ONLY for demo/testing. Remove before real production deployment.
NEXT_PUBLIC_DEMO_MODE=true
NEXT_PUBLIC_OPENROUTER_API_KEY=sk-or-...

# Optional OpenRouter settings
NEXT_PUBLIC_OPENROUTER_DEFAULT_MODEL=openai/gpt-4o-mini
NEXT_PUBLIC_OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
NEXT_PUBLIC_OPENROUTER_SITE_URL=http://localhost:3000
NEXT_PUBLIC_OPENROUTER_APP_NAME=AI Interface

# Existing backend base URL for chat CRUD endpoints (if used)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
