# public/

Static assets served from the site root.

- `Logo.png` is the canonical application logo.
- `Logo.svg` is the tracked vector fallback.
- `app/icon.png` and `app/apple-icon.png` provide the Next.js favicon metadata.
